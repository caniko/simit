[package]
name = "beta"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"

[dependencies]
alpha = { version = "0.1.0", path = "../alpha" }

[features]
default = ["std"]
std = []
